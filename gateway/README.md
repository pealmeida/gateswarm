# gateway/ — Legacy v0.4.4 reference (DO NOT USE FOR ACTIVE DEVELOPMENT)

⚠️ **This directory contains the legacy v0.4.4 "gateswarm-gateway" code.**
It is kept **for historical reference only** and is **not** the active codebase.

The active codebase is at the repository root (`src/`, `cli/`, `tests/`, etc.) and is
on version **0.5.4+**. See [`README.md`](../README.md) and [`docs/OPS_GUIDE.md`](../docs/OPS_GUIDE.md)
for the current setup.

## Why this directory exists

The original v0.5.1 release shipped from this `gateway/` subdirectory. The
v0.5.2 rewrite moved the active code to the repo root, but kept `gateway/`
intact as a reference for the v0.4 → v0.5 architecture transition.

If you find yourself editing files here, **stop and read the README first**.
The active code is one level up.

## Last known state

- **Version:** 0.4.4 (frozen)
- **Date:** 2026-05-26
- **Status:** archived; do not run, do not deploy
