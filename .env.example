# GateSwarm MoMA Router - Environment Variables
# Copy this file to .env and fill in your API keys.
# .env is gitignored; never commit real keys.

# Alibaba Bailian (Coding Plan) — optional
BAILIAN_KEY=sk-sp-xxxxx
BAILIAN_BASE=https://coding-intl.dashscope.aliyuncs.com/v1

# Z.AI (GLM Coding Lite) — required primary
GLM_API_KEY=your-api-key-here
ZAI_KEY=your-api-key-here
ZAI_BASE=https://api.z.ai/api/coding/paas/v4

# OpenCode Go (multi-model) — optional, used for free-tier fallbacks
OPENCODEGO_BASE=https://opencode.ai/zen/go/v1
OPENCODEGO_KEY=your-opencodego-key

# Ollama Cloud (free tier) — optional
OLLAMA_CLOUD_BASE=https://ollama.com/v1
OLLAMA_CLOUD_KEY=your-ollama-cloud-key

# Ollama (local CPU) — optional, defaults to localhost:11434
OLLAMA_BASE=http://127.0.0.1:11434/v1
OLLAMA_KEY=ollama

# Gateway
PORT=8900

# ─── Network exposure & auth ────────────────────────────────
# The gateway binds 127.0.0.1 by default (local only). To expose it on a
# network, set GATESWARM_HOST=0.0.0.0. When exposing beyond localhost,
# MOMA_ADMIN_TOKEN is required unless MOMA_ALLOW_UNAUTHENTICATED_ADMIN=true is
# explicitly set. Also enable GATESWARM_REQUIRE_AUTH so requests present a valid
# agent key (see `gateswarm agents`); unknown/absent keys get 401.
# GATESWARM_HOST=127.0.0.1
# GATESWARM_REQUIRE_AUTH=false
# MOMA_ADMIN_TOKEN=replace-with-a-long-random-token  # required when exposing beyond localhost
# MOMA_MAX_BODY_BYTES=1048576                         # required when exposing beyond localhost: set an explicit request cap

# Auto-detected: the gateway uses this to locate scripts/cli-health-probe.sh
# In systemd it's set in the service unit; for local dev, set it to your repo root.
# GATESWARM_ROOT=/path/to/gateswarm-router

# ─── Sovereign Vault (recommended) ──────────────────────────
# Store the keys above in Sovereign Vault instead of this plaintext file:
# fill .env once, run `node scripts/sv-import-env.mjs`, then delete .env.
# The gateway loads keys vault-first with .env as fallback (src/secrets/README.md).
# SECRETS_SOURCE=auto        # auto | vault | env
# SV_CONTAINER=env-gateswarm
# SV_FILE=.env
# SV_BIN=/abs/path/to/sovereign-vault
# SV_TIMEOUT_MS=30000
